#! /bin/bash

# This script searches through stalled git repositories.
set -e

cd $HOME/repo/stalled
choice=$(/bin/ls \
    | sort -u \
    | fzf)
cd - > /dev/null
[[ -n "$choice" ]] \
    && cd "$HOME/repo/stalled/$choice" && $SHELL
